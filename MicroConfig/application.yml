spring: 
 profiles:
  active:
  - dev
--- #测试
spring:
   profiles: test
   application:
    name: Micro-Service-Skeleton-dev
--- # 权限认证
server:
  port: 9060
spring:
  profiles: auth
  application:
    name: auth
  zipkin:
      base-url: http://localhost:9050
  jpa:
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/zuul-auth?useUnicode=true&characterEncoding=utf-8
    username: root
    password: qwer
    druid:
      driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: 127.0.0.1
    port: 6379
#    password: 
eureka:
  instance:
    prefer-ip-address: true #ʹ��IPע��
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
    ##续约更新时间间隔设置5秒，m默认30s
    lease-renewal-interval-in-seconds: 5
    ##续约到期时间10秒，默认是90秒
    lease-expiration-duration-in-seconds: 10
  client:
      service-url:
        defaultZone: http://server1:1000/eureka/,http://server2:999/eureka/
        
endpoints:
  health:
    sensitive: false
    enabled: true
management:
  security:
    enabled: false
security:
  oauth2:
    resource:
      filter-order: 3
logging:
    path:  ../logs # 保存日志文件目录路径
    pattern:
        console:  <%d{yyyy-MM-dd HH:mm}> === -->%msg%n # 控制台日志输出格式
        file: <%d{yyyy-MM-dd HH:mm}> === -->%msg%n # 文件日志输出格式
    level:
        com.neo: info #日志级别
--- # 网关
spring:
  profiles: gateway
  application:
    name: mss-gateway
  zipkin:
    base-url: http://localhost:9050
server:
  port: 9030
eureka:
  instance:
    prefer-ip-address: true #使用IP注册
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
    ##续约更新时间间隔设置5秒，m默认30s
    lease-renewal-interval-in-seconds: 5
    ##续约到期时间10秒，默认是90秒
    lease-expiration-duration-in-seconds: 10
  client:
      service-url:
        defaultZone: http://server1:1000/eureka/#,http://server2:999/eureka/
###actuator监控点 start####
endpoints:
  health:
    sensitive: false
    enabled: true
##默认情况下很多端点是不允许访问的，会返回401:Unauthorized
management:
  security:
    enabled: false
###actuator监控点 end####

###如下Oauth2.0配置参考##
###https://stackoverflow.com/questions/30327269/spring-oauth-authorization-server-behind-spring-cloud-zuul-proxy##
###https://github.com/spring-guides/tut-spring-security-and-angular-js/blob/master/oauth2-vanilla/README.adoc##
###http://wiselyman.iteye.com/blog/2379419##
#######
zuul:
  sensitive-headers:
  host:
    connect-timeout-millis: 10000
    socket-timeout-millis: 60000
  routes:
    uaa:
      path: /auth/**
      strip-prefix: true
      sensitiveHeaders:
      serviceId: auth
security:
  basic:
    enabled: false
  oauth2:
    client:
      ##网关的地址
      access-token-uri: http://localhost:9030/auth/oauth/token
      user-authorization-uri: http://localhost:9030/auth/oauth/authorize
    resource:
      user-info-uri:  http://localhost:9030/auth/user
      prefer-token-info: false
##############end#####################
####超时配置####
ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000
  MaxAutoRetries: 1
  MaxAutoRetriesNextServer: 2
  eureka:
    enabled: true
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 600000
###超时配置###